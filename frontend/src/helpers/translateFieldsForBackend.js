// I might not even need this helper function.
//   I'll keep it here, in case I do need to write it.

export function translateFieldsForBackend(fieldName) {
  switch (fieldName) {
    //Enter the field names to 'translate' here:
    default:
      return null;
  }
}
